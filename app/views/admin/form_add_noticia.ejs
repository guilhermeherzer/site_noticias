<!DOCTYPE html>
	<html lang="pt-br">
		<head>
			<meta charset="utf-8"/>
			<title>Formulário de cadastro de Notícia</title>
		</head>
	<body>

		<h1>Adicionar notícia</h1>
		<br>
		<% if(error){ %>
			<ul>
				<% for(var i = 0; i < error.length; i++){ %>
					<li><%= error[i].msg %></li>
				<% } %>
			</ul>
		<% } %>
		<form action="/noticia/salvar" method="post">
			<label>Titulo</label>
			<input name="titulo" value="<%= noticia.titulo %>">
			<br>
			<label>Resumo</label>
			<input name="resumo" value="<%= noticia.resumo %>">
			<br>
			<label>Autor</label>
			<input name="autor" value="<%= noticia.autor %>">
			<br>
			<label>Noticia</label>
			<textarea name="noticia" rows="5" cols="30"><%= noticia.noticia %></textarea>
			<br>
			<input type="submit" value="Enviar">
		</form>

	</body>
</html>